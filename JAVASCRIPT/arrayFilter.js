const array = [2, 3, 4, 5, 6, 7];

console.log(array.filter(valor => valor % 2 === 0)); //Imprime um novo array: [2, 4, 6]

/*
    array.map(callback, thisArgs)

    callback(valorItem, index, array)
*/